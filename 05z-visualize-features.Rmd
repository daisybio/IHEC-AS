---
title: "Feature visualization (DEPRECATED)"
output: html_document
---

```{r, deprecated}
# DEPRECATED 2026-07-10 — folded into 05b-feature-pca-sanity.Rmd.
#
# The PCA/UMAP embedding gallery (per Event Type × transcript_filter ×
# Variability incl. "Both", colored by ontology[palette]/project/protocol/PSI/
# chromosome + top PC1/PC2 loadings) now lives in 05b's `feature-gallery` chunk,
# alongside the quantitative sanity gates. 05b is per-filter
# (TRANSCRIPT_FILTER env; reads aggregated_dt_filtered_{tf}.csv.gz), whereas this
# file still read the removed combined aggregated_dt_filtered.csv.gz.
#
# The dormant `plot features` chunk here (feature correlation heatmap + per-mark
# distribution densities, always `eval=FALSE`) was never executed and was not
# ported; revive it in 05b if those views are ever wanted.
stop(
  "05z-visualize-features.Rmd is deprecated — use 05b-feature-pca-sanity.Rmd ",
  "(feature-gallery chunk)."
)
```
