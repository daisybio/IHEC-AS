---
title: "preliminary-results"
output:
  html_document:
    df_print: paged
---

```{r, general metadata}
file_metadata <- fread("processed_data/file_table.csv.gz")
metadata <- data.table::fread(sample_metadata_file)
metadata <- metadata[epirr_id_without_version %in% file_metadata[, unique(epirr_id_without_version)]]
# TODO: this filter should be cleaner, together with file_metadata
# metadata <- metadata[`automated_experiments_RNA-Seq_mRNA-Seq` %in% sample_cols | `automated_experiments_RNA-Seq_total-RNA-Seq` %in% sample_cols]
bam_dt <- data.table(
  bam = c(
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009331/1.0.0/1.1.0/hg38_no_alt/75/8177/work/f4/ff34e2e7a1b6216bbf8c59e26ae4f1/K009331_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009330/1.0.0/1.1.0/hg38_no_alt/75/8176/work/df/4d3fa1685efcf134929b12b9e4f549/K009330_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009333/1.0.0/1.1.0/hg38_no_alt/75/8175/work/c8/cade5167cf026da3936366fc259718/K009333_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009332/1.0.0/1.1.0/hg38_no_alt/75/8174/work/65/9dd6bf34e3471ee9c22a2b1e5146a2/K009332_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009334/1.0.0/1.1.0/hg38_no_alt/75/8167/work/5e/d8b4bda598d2913427aef88915cffd/K009334_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009335/1.0.0/1.1.0/hg38_no_alt/75/8168/work/f3/3873664c8ce4a3af54a2ad0e384ea6/K009335_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009337/1.0.0/1.1.0/hg38_no_alt/75/8173/work/07/2b4827c201cb47633a59b2b9959845/K009337_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009336/1.0.0/1.1.0/hg38_no_alt/75/8172/work/02/1e59765267de0efbc517b570c11ed8/K009336_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009339/1.0.0/1.1.0/hg38_no_alt/75/8171/work/1c/8a1cd58f6e9e829e21e4b9a44b4322/K009339_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009338/1.0.0/1.1.0/hg38_no_alt/75/8170/work/85/4cfd38648a13da79969248d2febc39/K009338_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009341/1.0.0/1.1.0/hg38_no_alt/75/8179/work/9d/975f47eee547312ffa13a743637bca/K009341_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009340/1.0.0/1.1.0/hg38_no_alt/75/8178/work/0d/d5d92a6c288c8285ef39ace05434af/K009340_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009343/1.0.0/1.1.0/hg38_no_alt/75/8181/work/b5/9f398b50552276c3b282983dc537d4/K009343_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009342/1.0.0/1.1.0/hg38_no_alt/75/8180/work/e8/8e03bbd5675b7e4d15bef907d44840/K009342_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009345/1.0.0/1.1.0/hg38_no_alt/75/8183/work/88/2d179ca34731b49671ac9965ba5591/K009345_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009344/1.0.0/1.1.0/hg38_no_alt/75/8182/work/6a/c3f7a5f2bd95c34a4d057cee451b10/K009344_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009347/1.0.0/1.1.0/hg38_no_alt/75/8185/work/2a/7e107c95b4dc215f2c8ea24298c96f/K009347_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009346/1.0.0/1.1.0/hg38_no_alt/75/8184/work/33/852a5b510bc382fbd14b030040f5ec/K009346_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009349/1.0.0/1.1.0/hg38_no_alt/75/8187/work/ba/510ecfe57780797980928b0cf4a05d/K009349_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009348/1.0.0/1.1.0/hg38_no_alt/75/8186/work/52/b8bf5b82bbe29e42d2e031530a8be4/K009348_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009351/1.0.0/1.1.0/hg38_no_alt/75/8188/work/ed/a9edeb002a80e66be0806f3cb76148/K009351_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009350/1.0.0/1.1.0/hg38_no_alt/75/8202/work/4f/dea973195470756c5f863602ab0120/K009350_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009352/1.0.0/1.1.0/hg38_no_alt/75/8189/work/be/fa20d950f8dad490bf50a0e96f7308/K009352_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009354/1.0.0/1.1.0/hg38_no_alt/75/8191/work/d3/b42747e0d28b9c77580e4b61968fc1/K009354_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009353/1.0.0/1.1.0/hg38_no_alt/75/8190/work/3a/d2e4e2914d351492a092231153ce89/K009353_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009356/1.0.0/1.1.0/hg38_no_alt/75/8193/work/d8/c76ab062480d6955faa0800b6da841/K009356_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009355/1.0.0/1.1.0/hg38_no_alt/75/8192/work/3d/6de682131a37684e32c7528549e324/K009355_m4_n10_toGenome.markdup.bam",
    "/projects/deep_int_dart2/IHEC/IA/Deep/rnaseq/K009358/1.0.0/1.1.0/hg38_no_alt/75/8195/work/a7/7f9e9b3f840e1eaaa7c762a2a957fc/K009358_m4_n10_toGenome.markdup.bam"
  )
)
bam_dt[, path := tstrsplit(bam, "/work", keep = 1)]
bam_mapping <- fread("rmats_mapping/DEEP.RNA_Seq.EpiRRID_exptype_path.IHEC.tsv", header = FALSE)
setnames(bam_mapping, 1:4, c("project", "epirr", "experiment_type", "path"))
bam_dt[bam_mapping, on = .NATURAL, c("epirr", "experiment_type") := .(epirr, experiment_type)]
bam_dt[, sample_name := paste(epirr, experiment_type, sep = "_")]
experiment_metadata <- fread("data/EpiATLAS_experiment_metadata.csv")
bam_dt[experiment_metadata[, epirr := epirr_id], on = .NATURAL, uuid := uuid]
```


```{r, filter_events_per_method, fig.width=16, fig.height=12}
library(UpSetR)
library(grid)
library(patchwork)

min_average_PSI <- .05
max_average_PSI <- 1 - min_average_PSI
min_average_count <- 20
min_average_tpm <- 1

# first read the samples that have been quantified
suppa_header <- readLines("suppa_analysis/tpm_expressions_filtered.tsv", n = 1)
suppa_sample_cols <- strsplit(suppa_header, "\t", TRUE)[[1]]
suppa_psi_dir <- "suppa_analysis/events_filtered"

rmats_dir <- "rmats_out"
rmats_file_ending <- ".MATS.JC.txt"

filtered_events <- sapply(as_events, function(event_type) {
  ### Read SUPPA events ----
  suppa_event_file_name <- paste0("event_", event_type)
  suppa_psi_file <-
    file.path(suppa_psi_dir, paste0(suppa_event_file_name, ".psi"))
  # stores the summed TPM expression of all transcripts involved in the event
  suppa_tpm_file <-
    file.path(suppa_psi_dir, paste0(suppa_event_file_name, ".tpm"))

  suppa_dt <-
    rbindlist(sapply(list(tpm = suppa_tpm_file, psi = suppa_psi_file), function(f) {
      dt <- fread(f)
      setnames(dt, "V1", "event_id")
      melt(
        dt,
        id.vars = "event_id",
        variable.name = "uuid",
        value.name = "value",
        variable.factor = TRUE
      )
    }, simplify = FALSE), idcol = "type")

  ### Read rMATS events ----
  rmats_file_pattern <- paste0(event_type, ".*", rmats_file_ending)
  rmats_file <-
    list.files(
      path = rmats_dir,
      pattern = rmats_file_pattern,
      full.names = TRUE
    )
  rmats_dt <- fread(rmats_file)
  if (event_type == "SE") {
    rmats_dt[, event_id := sprintf(
      "%s;%s:%s:%s-%s:%s-%s:%s",
      GeneID,
      event_type,
      chr,
      upstreamEE,
      exonStart_0base + 1,
      exonEnd,
      downstreamES + 1,
      strand
    )]
  } else if (event_type == "RI") {
    rmats_dt[, event_id := sprintf(
      "%s;%s:%s:%s:%s-%s:%s:%s",
      GeneID,
      event_type,
      chr,
      upstreamES + 1,
      upstreamEE,
      downstreamES + 1,
      downstreamEE,
      strand
    )]
  } else if (event_type == "A3") {
    rmats_dt[, event_id := sprintf(
      "%s;%s:%s:%s-%s:%s-%s:%s",
      GeneID,
      event_type,
      chr,
      ifelse(strand == "+", flankingEE, longExonEnd),
      ifelse(strand == "+", longExonStart_0base + 1, flankingES + 1),
      ifelse(strand == "+", flankingEE, shortEE),
      ifelse(strand == "+", shortES + 1, flankingES + 1),
      strand
    )]
  } else if (event_type == "A5") {
    rmats_dt[, event_id := sprintf(
      "%s;%s:%s:%s-%s:%s-%s:%s",
      GeneID,
      event_type,
      chr,
      ifelse(strand == "+", longExonEnd, flankingEE),
      ifelse(strand == "+", flankingES + 1, longExonStart_0base + 1),
      ifelse(strand == "+", shortEE, flankingEE),
      ifelse(strand == "+", flankingES + 1, shortES + 1),
      strand
    )]
  } else if (event_type == "MX") {
    rmats_dt[, event_id := sprintf(
      "%s;%s:%s:%s-%s:%s-%s:%s-%s:%s-%s:%s",
      GeneID,
      event_type,
      chr,
      upstreamEE,
      `1stExonStart_0base` + 1,
      `1stExonEnd`,
      downstreamES + 1,
      upstreamEE,
      `2ndExonStart_0base` + 1,
      `2ndExonEnd`,
      downstreamES + 1,
      strand
    )]
  } else {
    stop("Not implemented yet")
  }
  # convert to long format which still has comma-separated values
  rmats_dt <- melt(rmats_dt, id.vars = c("event_id"), measure.vars = c("IJC_SAMPLE_1", "SJC_SAMPLE_1", "IncLevel1"), value.name = "value", variable.name = "type", variable.factor = TRUE)
  # add sample names, by splitting by comma and retaining the order of which they appear in the rmats input file
  rmats_dt[, bam_dt[, uuid] := tstrsplit(value, ",", fixed = TRUE, type.convert = TRUE)]
  # convert to real long format to have one row per sample
  rmats_dt <-
    melt(
      rmats_dt,
      id.vars = c("type", "event_id"),
      measure.vars = bam_dt[, uuid],
      variable.name = "uuid"
    )
  # make sure counts are non-NA
  rmats_dt[type != "IncLevel1", stopifnot(!is.na(value))]
  # rename columns
  rmats_dt[type == "IncLevel1", type := "psi"]
  rmats_dt[type != "psi", type := "count"]
  rmats_dt[, type := droplevels(type)]
  # for filtering we do not need to keep track of which count it is (inclusion or skipping)
  rmats_dt <- rmats_dt[, .(value = sum(value)), by = .(type, event_id, uuid)]

  #### Merge the tables ----
  event_dt <- rbindlist(list(rmats = rmats_dt, suppa = suppa_dt), idcol = "method")
  event_dt[, method := as.factor(method)]
  event_dt[, type := factor(type, levels = c("psi", "count", "tpm"))]
  event_dt[file_metadata, on = .(uuid), c("experiment_type", "epirr_id_without_version") := .(i.experiment_type, i.epirr_id_without_version)]
  event_dt[, experiment_type := as.factor(experiment_type)]
  # remove the uuids that do not have metadata associated
  warning(sprintf("Removing samples that do not have metadata associated: %s", event_dt[is.na(experiment_type), paste(unique(uuid), collapse = ", ")]))
  event_dt <- event_dt[!is.na(experiment_type)]
  # filter based on sd right away

  # create averages per event, method, type, (experiment_type)
  average_dt <- rbind(event_dt[, .(mean = mean(value, na.rm = TRUE), sd = sd(value, na.rm = TRUE)), by = .(method, type, event_id)],
    event_dt[, .(mean = mean(value, na.rm = TRUE), sd = sd(value, na.rm = TRUE)), by = .(method, type, event_id, experiment_type)],
    fill = TRUE
  )
  # TODO: do this on the initial table
  # if all psi values are the same or there is only one in a group, the filter is automatically not passes
  average_dt[type == "psi" & (is.na(sd) | sd == 0), passes_filter := FALSE]
  # apply the actual filters
  average_dt[type == "psi", passes_filter := (mean >= min_average_PSI & mean <= max_average_PSI)]
  average_dt[type == "count", passes_filter := (mean >= min_average_count)]
  average_dt[type == "tpm", passes_filter := (mean >= min_average_tpm)]
  # if PSI is always NA, no mean can be computed, clearly they do not pass filter
  average_dt[is.na(passes_filter), stopifnot(type == "psi")]
  average_dt[is.na(passes_filter), passes_filter := FALSE]
  # to pass the filter, when grouped by experiment_type, all groups must pass the filter
  average_dt[!is.na(experiment_type), passes_filter := all(passes_filter), by = .(method, type, event_id)]
  average_dt[, filter_string := ifelse(passes_filter, "passes filter", "fails filter")]
  average_dt[, filter_string := factor(filter_string, levels = c("passes filter", "fails filter"))]
  average_dt[!is.na(experiment_type), event_filter :=
    # because we want it in suppa and rmats (2) with two measures each (2) and in both experiment_types (2)
    # 2 * 2 * 2
    ifelse(sum(passes_filter) == 8,
      "event passes", "event filtered"
    ), by = event_id]
  average_dt[, event_filter := factor(event_filter)]
  print(sprintf("Passed events for %s: %d", event_type, average_dt[event_filter == "event passes", uniqueN(event_id)]))

  filter_vs_passed <-
    ggplot(
      average_dt,
      aes(y = mean + 1, x = filter_string, color = experiment_type)
    ) +
    geom_hline(aes(yintercept = yinterc),
      data = data.frame(
        yinterc = c(
          min_average_tpm + 1,
          min_average_count + 1,
          min_average_PSI + 1,
          max_average_PSI + 1,
          min_average_PSI + 1,
          max_average_PSI + 1
        ),
        type = c("tpm", "count", "psi", "psi", "psi", "psi"),
        method = c("suppa", "rmats", "suppa", "suppa", "rmats", "rmats")
      )
    ) +
    geom_boxplot() +
    stat_summary(
      fun.data = function(x) {
        c(y = 0, label = length(x))
      },
      geom = "label",
      position = position_dodge(.75)
    ) +
    facet_wrap(~ factor(type, levels = c("psi", "count", "tpm")) + method, scales = "free") +
    scale_y_log10()

  psi_dist_passed <-
    ggplot(
      event_dt[type == "psi" &
        event_id %in% average_dt[
          event_filter == "event passes",
          unique(event_id)
        ]],
      aes(x = method, y = value, color = experiment_type)
    ) +
    geom_violin() +
    labs(title = "all passed events") +
    theme(legend.position = "none")

  print(filter_vs_passed + psi_dist_passed + plot_layout(widths = c(2, 1)))

  # # distributions:
  # print(
  #   ggplot(average_dt[type == "psi"], aes(x = mean, fill = experiment_type)) +
  #     geom_histogram(position = "dodge", binwidth = .05) +
  #     geom_vline(xintercept = c(min_average_PSI, max_average_PSI)) +
  #     labs(title = sprintf("Average PSI: %s, %f <= PSI < %f", event_type, min_average_PSI, max_average_PSI)) +
  #     facet_wrap(method ~ filter_string, scales = "free_y")
  # )
  # print(
  #   ggplot(average_dt[type != "psi"], aes(x = mean + 1, color = experiment_type)) +
  #     geom_line(aes(y = 1 - after_stat(y)), stat = "ecdf") +
  #     scale_x_log10() +
  #     geom_vline(
  #       aes(xintercept = xinterc),
  #       data = data.frame(
  #         xinterc = c(min_average_tpm + 1, min_average_count + 1),
  #         type = c("tpm", "count")
  #       )
  #     ) +
  #     labs(
  #       title = sprintf(
  #         "%s: Average Read Coverage at %d and %d for counts and TPM respectively",
  #         event_type,
  #         min_average_count,
  #         min_average_tpm
  #       )
  #     ) + facet_grid(~ type, scales = "free_y")
  # )

  # Define your cutoff grid
  min_tpm_cutoffs <- seq(0, 1, by = 0.2)
  min_count_cutoffs <- c(0, 1, 5, 10, 15, 20)
  min_psi_cutoffs <- seq(0, 0.05, by = 0.01)

  cutoff_grid <- CJ(
    min_average_tpm = min_tpm_cutoffs,
    min_average_count = min_count_cutoffs,
    min_average_PSI = min_psi_cutoffs
  )

  # Prepare wide format tables
  # TODO: there is so SD filter in here
  rmats_average_dt <- dcast(
    average_dt[!is.na(experiment_type) &
      method == "rmats"],
    event_id + experiment_type ~ type,
    value.var = "mean"
  )

  suppa_average_dt <- dcast(
    average_dt[!is.na(experiment_type) &
      method == "suppa"],
    event_id + experiment_type ~ type,
    value.var = "mean"
  )

  # Define the filtering function
  count_overlap <- function(cutoff_row) {
    min_tpm <- cutoff_row[["min_average_tpm"]]
    min_count <- cutoff_row[["min_average_count"]]
    min_psi <- cutoff_row[["min_average_PSI"]]

    rmats_filt <- rmats_average_dt[
      (psi >= min_psi) &
        (psi <= (1 - min_psi)) & (count >= min_count),
      event_id,
      by = experiment_type
    ][duplicated(event_id), event_id]

    suppa_filt <- suppa_average_dt[
      (psi >= min_psi) &
        (psi <= (1 - min_psi)) & (tpm >= min_tpm),
      event_id,
      by = experiment_type
    ][duplicated(event_id), event_id]

    n_common <- length(intersect(rmats_filt, suppa_filt))
    return(
      data.table(
        min_average_tpm = min_tpm,
        min_average_count = min_count,
        min_average_PSI = min_psi,
        n_pass = n_common
      )
    )
  }

  # Run in parallel
  results_list <- pbmclapply(
    seq_len(nrow(cutoff_grid)),
    function(i) {
      count_overlap(cutoff_grid[i])
    }
  )

  # Combine results
  plot_dt <- rbindlist(results_list)

  print(ggplot(
    plot_dt,
    aes(
      x = min_average_PSI,
      y = n_pass,
      color = factor(min_average_count)
    )
  ) +
    geom_point() +
    geom_line() +
    labs(title = sprintf("Remaining after filtering faceted by tpm filter: %s", event_type)) +
    facet_wrap(~min_average_tpm))

  passed_event_dt <- event_dt[type == "psi" & event_id %in% average_dt[event_filter == "event passes", unique(event_id)]]
  wide_passed_event_dt <- dcast(passed_event_dt, event_id + uuid + experiment_type + epirr_id_without_version ~ method, value.var = "value")
  random_scatter <- ggplot(wide_passed_event_dt[event_id %in% na.omit(wide_passed_event_dt)[, sample(unique(event_id), 16)]], aes(x = rmats, y = suppa, color = experiment_type)) +
    geom_abline(linetype = "dashed", color = "grey") +
    geom_point() +
    facet_wrap(~event_id) +
    coord_fixed()

  cor_dt <- rbindlist(sapply(c("pearson", "spearman"), function(my_coef) na.omit(wide_passed_event_dt)[, .(correlation = cor(rmats, suppa, method = my_coef)), by = "event_id"], simplify = FALSE), idcol = "coef")
  cor_plot <- ggplot(cor_dt, aes(x = coef, y = correlation)) +
    geom_boxplot() +
    labs(title = sprintf("Correlation between RMATS and SUPPA PSI: %s", event_type))
  print(random_scatter + cor_plot + plot_layout(widths = c(2, 1)))

  passed_event_dt
}, simplify = FALSE)
```

```{r, load-events}
event_res <- sapply(as_events, function(event_type) {
  # get the data for this event type
  long_dt <- filtered_events[[event_type]]
  # load psi values
  event_dt <- dcast(long_dt, event_id ~ method + uuid)
  sample_cols <- names(event_dt)[names(event_dt) != "event_id"]

  # prepare the alternative/upstream/downstream regions.
  # needs some data wrangling depending on the event type but basically we just parse the SUPPA event id
  # some information only needed for SE and RI and deleted for the other types
  if (event_type == "SE") {
    event_dt[, c("Event Type", "seqnames", "e1s2", "e2s3", "strand") := tstrsplit(event_id, ":", fixed = TRUE)]
    event_dt[, c("gene_id", "event_type") := tstrsplit(`Event Type`, ";", fixed = T)]
    event_dt[, c("e1", "s2", "e2", "s3") := c(
      tstrsplit(e1s2, "-", fixed = TRUE, type.convert = TRUE),
      tstrsplit(e2s3, "-", fixed = TRUE, type.convert = TRUE)
    )]
    all_gr <-
      event_dt[, GRanges(
        seqnames = seqnames,
        ranges = IRanges(start = e1, end = s3),
        strand = strand
      )]
    upstream_gr <-
      event_dt[, GRanges(
        seqnames = seqnames,
        ranges = IRanges(
          start = ifelse(strand == "+", e1, e2) + 1,
          end = ifelse(strand == "+", s2, s3) - 1
        ),
        strand = strand
      )]
    downstream_gr <-
      event_dt[, GRanges(
        seqnames = seqnames,
        ranges = IRanges(
          start = ifelse(strand == "+", e2, e1) + 1,
          end = ifelse(strand == "+", s3, s2) - 1
        ),
        strand = strand
      )]
    event_gr <-
      event_dt[, GRanges(
        seqnames = seqnames,
        ranges = IRanges(start = s2, end = e2),
        strand = strand
      )]
  } else if (event_type == "RI") {
    event_dt[, c("Event Type", "seqnames", "s1", "e1s2", "e2", "strand") := tstrsplit(event_id, ":", fixed = TRUE, type.convert = TRUE)]
    event_dt[, c("gene_id", "event_type") := tstrsplit(`Event Type`, ";", fixed = T)]
    event_dt[, c("e1", "s2") := tstrsplit(e1s2, "-", fixed = TRUE, type.convert = TRUE)]
    all_gr <-
      event_dt[, GRanges(
        seqnames = seqnames,
        ranges = IRanges(start = s1, end = e2),
        strand = strand
      )]
    upstream_gr <-
      event_dt[, GRanges(
        seqnames = seqnames,
        ranges = IRanges(
          start = ifelse(strand == "+", s1, s2),
          end = ifelse(strand == "+", e2, e2)
        ),
        strand = strand
      )]
    downstream_gr <-
      event_dt[, GRanges(
        seqnames = seqnames,
        ranges = IRanges(
          start = ifelse(strand == "+", s2, s1),
          end = ifelse(strand == "+", e2, e1)
        ),
        strand = strand
      )]
    event_gr <-
      event_dt[, GRanges(
        seqnames = seqnames,
        ranges = IRanges(start = e1 + 1, end = s2 - 1),
        strand = strand
      )]
  } else if (event_type %in% c("AL", "AF")) {
    event_dt[, c(
      "Event Type",
      "seqnames",
      "s1/e1s2",
      "e1s3/e2",
      "s2/e1s3",
      "e2s3/e3",
      "strand"
    ) := tstrsplit(event_id, ":", fixed = TRUE, type.convert = TRUE)]
    event_dt[, c("gene_id", "event_type") := tstrsplit(`Event Type`, ";", fixed = T)]
    event_dt[ifelse(event_type == "AF", strand == "+", strand == "-"), start_all := as.integer(`s1/e1s2`)]
    event_dt[ifelse(event_type == "AF", strand == "-", strand == "+"), start_all := tstrsplit(`s1/e1s2`, "-", fixed = TRUE, type.convert = TRUE)[[1]] + 1]
    event_dt[ifelse(event_type == "AF", strand == "-", strand == "+"), end_all := as.integer(`e2s3/e3`)]
    event_dt[ifelse(event_type == "AF", strand == "+", strand == "-"), end_all := tstrsplit(`e2s3/e3`, "-", fixed = TRUE, type.convert = TRUE)[[2]] - 1]
    all_gr <-
      event_dt[, GRanges(
        seqnames = seqnames,
        ranges = IRanges(start = start_all, end = end_all),
        strand = strand
      )]
    upstream_gr <- NULL
    downstream_gr <- NULL
    event_gr <- NULL
  } else if (event_type %in% c("A3", "A5")) {
    event_dt[, c("Event Type", "seqnames", "e1s2/e2s3", "e1s3", "strand") := tstrsplit(event_id, ":", fixed = TRUE, type.convert = TRUE)]
    event_dt[, c("gene_id", "event_type") := tstrsplit(`Event Type`, ";", fixed = T)]
    event_dt[, c("e1/e2", "s2/s3") := tstrsplit(`e1s2/e2s3`,
      "-",
      fixed = TRUE,
      type.convert = TRUE
    )]
    event_dt[, c("e1", "s3") := tstrsplit(e1s3, "-", fixed = TRUE, type.convert = TRUE)]
    all_gr <-
      event_dt[, GRanges(
        seqnames = seqnames,
        ranges = IRanges(start = `e1/e2` + 1, end = s3 - 1),
        strand = strand
      )]
    upstream_gr <- NULL
    downstream_gr <- NULL
    event_gr <- NULL
  } else if (event_type == "MX") {
    event_dt[, c(
      "Event Type",
      "seqnames",
      "e1s2",
      "e2s4",
      "e1s3",
      "e3s4",
      "strand"
    ) := tstrsplit(event_id, ":", fixed = TRUE, type.convert = TRUE)]
    event_dt[, c("gene_id", "event_type") := tstrsplit(`Event Type`, ";", fixed = T)]
    event_dt[, start_all := tstrsplit(e1s2, "-", fixed = TRUE, type.convert = TRUE)[[1]] + 1]
    event_dt[, end_all := tstrsplit(e3s4, "-", fixed = TRUE, type.convert = TRUE)[[2]] - 1]
    all_gr <-
      event_dt[, GRanges(
        seqnames = seqnames,
        ranges = IRanges(start = start_all, end = end_all),
        strand = strand
      )]
    upstream_gr <- NULL
    downstream_gr <- NULL
    event_gr <- NULL
  } else {
    stop("unknown event")
  }

  # return the results in a list
  list(
    event_dt = event_dt[, c("event_id", "gene_id", "seqnames", "strand", ..sample_cols)],
    # dt with event info
    event_gr = event_gr,
    # gr of alternative region
    upstream_gr = upstream_gr,
    # gr of upstream region
    downstream_gr = downstream_gr,
    # gr of downstream region
    all_gr = all_gr # merged gr
  )
}, simplify = FALSE)
```

```{r, fig.width=7, fig.height=4, event-filtering}
# make one large GRanges object with all events
all_gr <- Reduce(c, lapply(event_res, function(event) {
  event$all_gr
}))

# stack event_dts and gr objects for SE and RI events
event_dt <- data.table()
event_gr <- GRanges()
upstream_gr <- GRanges()
downstream_gr <- GRanges()
for (this_event in to_analyze) {
  this_dt <- copy(event_res[[this_event]]$event_dt)
  this_event_gr <- event_res[[this_event]]$event_gr
  # filter out events with multiple genes
  single_genes_only <-
    this_dt[!grepl("_", gene_id, fixed = TRUE), which = TRUE]

  # filter out events on gonosomes
  not_gonosomes <-
    this_dt[!seqnames %in% c("chrY", "chrX"), which = TRUE]

  # find events that overlap with the same event type or other event types
  other_event_overlaps <- findOverlaps(this_event_gr, all_gr)
  non_overlapping_other <-
    other_event_overlaps@from[which(!(
      duplicated(other_event_overlaps@from) |
        duplicated(other_event_overlaps@from, fromLast = TRUE)
    ))]
  self_event_overlaps <- findOverlaps(this_event_gr)
  non_overlapping_self <-
    self_event_overlaps@from[which(!(
      duplicated(self_event_overlaps@from) |
        duplicated(self_event_overlaps@from, fromLast = TRUE)
    ))]
  # plot how many events remain per filtering for this event type
  pdf(
    file = file.path(
      plot_dir,
      paste0(this_event, "_event_intersections.pdf")
    ),
    width = 7,
    height = 5,
    onefile = FALSE
  )
  upset <-
    upset(
      fromList(
        list(
          "all" = this_dt[, seq.int(.N)],
          "no overlap other" = non_overlapping_other,
          "no overlap self" = non_overlapping_self,
          "single gene" = single_genes_only,
          "not gonosomes" = not_gonosomes
        )
      ),
      sets.bar.color = "#56B4E9",
      order.by = "freq"
    )
  print(upset)
  grid.text(
    paste(this_event),
    x = 0.65,
    y = 0.95,
    gp = gpar(fontsize = 20)
  )
  dev.off()

  # actually append the data to GRanges
  event_gr <- c(event_gr, this_event_gr)
  upstream_gr <- c(upstream_gr, event_res[[this_event]]$upstream_gr)
  downstream_gr <-
    c(downstream_gr, event_res[[this_event]]$downstream_gr)
  # actually append the data to data table
  this_dt[, `Event Type` := this_event]
  event_dt <- rbind(event_dt, this_dt)
}
rm(event_res)
# adjust ids to be unique indices
event_dt[, ID := seq.int(.N)]
event_dt[, `Event Type` := factor(`Event Type`, levels = to_analyze)] # make sure the order is correct

# Repeat filtering on big table/Granges object
other_event_overlaps <- findOverlaps(event_gr, all_gr)
non_overlapping_other <-
  other_event_overlaps@from[which(!(
    duplicated(other_event_overlaps@from) |
      duplicated(other_event_overlaps@from, fromLast = TRUE)
  ))]
single_genes_only <-
  event_dt[!grepl("_", gene_id, fixed = TRUE), which = TRUE]
not_gonosomes <-
  event_dt[!seqnames %in% c("chrY", "chrX"), which = TRUE]

# keep rows that are in all three sets
keep_rows <-
  intersect(
    intersect(single_genes_only, non_overlapping_other),
    not_gonosomes
  )

# include experimental events manually, to make sure models are built for this:
manual_ids <-
  c() # TODO new manual ids necessary? 4019, 4020, 460:463, 310:314, 8201:8202, 1658, 1048:1049, 13896:13897)

# make sure keep_rows and manual_ids are disjoint
stopifnot(length(intersect(keep_rows, manual_ids)) == 0)
keep_rows_manual <- unique(sort(c(keep_rows, manual_ids)))

# double check ids
cols_to_factor <- c("event_id", "gene_id", "seqnames", "strand")
event_dt[, (cols_to_factor) := lapply(.SD, as.factor), .SDcols = cols_to_factor]
```

Export sequences for MaxEntScan

```{bash, download-genome, eval = FALSE}
wget -q -O data/GCA_000001405.15_GRCh38_no_alt_analysis_set.fna.gz http://ftp.ncbi.nlm.nih.gov/genomes/archive/old_genbank/Eukaryotes/vertebrates_mammals/Homo_sapiens/GRCh38/seqs_for_alignment_pipelines/GCA_000001405.15_GRCh38_no_alt_analysis_set.fna.gz
```

```{r, export-sequences}
library(Biostrings)
genome_fasta <-
  "data/GCA_000001405.15_GRCh38_no_alt_analysis_set.fna.gz"
genome <- readDNAStringSet(genome_fasta) # load genome
chr_names <-
  tstrsplit(names(genome), "[ \t]+", keep = 1)[[1]] # split names by tab/space
names(genome) <- chr_names

# make new genomic ranges object from event_dt: create 5' 9mer 9 bases long. [3 bases in exon][6 bases in intron] for SE, we need to take the end here
down5ss <- promoters(downstream_gr, upstream = 3, downstream = 6)
event5ss <- promoters(event_gr, upstream = 3, downstream = 6)
is_se <- event_dt[, `Event Type` == "SE"]
se5ss_ranges <- down5ss[is_se]
ri5ss_ranges <- event5ss[!is_se]
ss5_indices <- order(c(which(is_se), which(!is_se)))
ss5_gr <- c(se5ss_ranges, ri5ss_ranges)[ss5_indices]

# extract actual sequences
ss5 <- genome[ss5_gr]
ss5[event_dt[, strand == "-"]] <-
  reverseComplement(ss5[event_dt[, strand == "-"]])
names(ss5) <- event_dt[, ID]
# remove Ns, because MaxEntScan can't handle them
ss5 <-
  ss5[sapply(vmatchPattern("N", ss5), function(x) {
    length(x) == 0
  })]
# write to file
ss5_file <- sprintf("processed_data/5ss.fasta")
writeXStringSet(ss5, filepath = ss5_file)

# make new genomic ranges object from event_dt: create 3' 23mer 23 bases long. [20 bases in the intron][3 base in the exon] for SE, we need to take the start here
event3ss <- promoters(event_gr, upstream = 20, downstream = 3)
down3ss <- promoters(downstream_gr, upstream = 20, downstream = 3)
se3ss_ranges <- event3ss[is_se]
ri3ss_ranges <- down3ss[!is_se]
ss3_indices <- order(c(which(is_se), which(!is_se)))
ss3_gr <- c(se3ss_ranges, ri3ss_ranges)[ss3_indices]

# extract actual sequences
ss3 <- genome[ss3_gr]
ss3[event_dt[, strand == "-"]] <-
  reverseComplement(ss3[event_dt[, strand == "-"]])
names(ss3) <- event_dt[, ID]
# remove Ns, because MaxEntScan can't handle them
ss3 <-
  ss3[sapply(vmatchPattern("N", ss3), function(x) {
    length(x) == 0
  })]
# write to file
ss3_file <- sprintf("processed_data/3ss.fasta")
writeXStringSet(ss3, filepath = ss3_file)

# define the ss3 and ss5 score files
ss3_score_file <-
  sprintf("processed_data/3scores.txt")
ss5_score_file <-
  sprintf("processed_data/5scores.txt")
#```

#```{bash, engine.opts="-l"}
system(
  sprintf(
    "bash -lc 'conda activate ihec-as && wget -q -O maxentscan.tar.gz http://hollywood.mit.edu/burgelab/maxent/download/fordownload.tar.gz && tar -xf maxentscan.tar.gz && mv -f fordownload maxentscan && rm maxentscan.tar.gz && cd maxentscan && perl score3.pl ../%s > ../%s && perl score5.pl ../%s > ../%s && cd .. && rm -r maxentscan'",
    ss3_file,
    ss3_score_file,
    ss5_file,
    ss5_score_file
  ),
  intern = TRUE
)
#```

#```{r, read-MaxEntScanScores}
# read the scores and match the to the events
scores3 <- fread(ss3_score_file)
names(scores3) <- c("sequence", "3ss")
header_3 <- grep("^>", readLines(ss3_file), value = TRUE)
scores3[, ID := as.integer(substr(header_3, 2, nchar(header_3)))]
scores3[, sequence := NULL]

# read the scores and match the to the events
scores5 <- fread(ss5_score_file)
names(scores5) <- c("sequence", "5ss")
header_5 <- grep("^>", readLines(ss5_file), value = TRUE)
scores5[, ID := as.integer(substr(header_5, 2, nchar(header_5)))]
scores5[, sequence := NULL]
```

```{r, add-intrinsic-features}
# load annotation
annotation <-
  rtracklayer::import(sprintf("suppa_analysis/gencode.v29.filtered.gtf"))

# load suppa results of inclusion and exclusion transcripts
ioes <-
  rbindlist(lapply(to_analyze, function(event_type) {
    fread(
      paste0(
        suppa_psi_dir,
        "/gencode.v29.filtered.events_",
        event_type,
        "_strict.ioe"
      )
    )
  }))
ioes[, alternative_transcripts := lapply(strsplit(alternative_transcripts, ",", fixed = TRUE), I)]
ioes[, total_transcripts := lapply(strsplit(total_transcripts, ",", fixed = TRUE), I)]
ioes[, reference_transcripts := mapply(
  function(a, t) {
    setdiff(t, a)
  },
  alternative_transcripts,
  total_transcripts
)]

# merge ioes into event_dt and add alternative_transcripts and total_transcripts to event_dt by reference
event_dt[ioes, on = .NATURAL, c(
  "alternative_transcripts",
  "reference_transcripts",
  "total_transcripts"
) := .(
  alternative_transcripts,
  reference_transcripts,
  total_transcripts
)]

# add transcript start and end to event_dt
event_transcripts <-
  event_dt[, .(transcript_id = unique(unlist(alternative_transcripts))), by = ID]
used_transcripts <-
  annotation[annotation$transcript_id %in% unique(event_transcripts$transcript_id) &
    annotation$type == "transcript"]
event_transcripts[
  data.table(
    transcript_id = used_transcripts$transcript_id,
    transcript_start = start(used_transcripts),
    transcript_end = end(used_transcripts)
  ),
  on = .NATURAL,
  c("transcript_start", "transcript_end") := .(transcript_start, transcript_end)
]

other_transcripts <-
  event_dt[, .(transcript_id = unique(unlist(reference_transcripts))), by = ID]
used_other_transcripts <-
  annotation[annotation$transcript_id %in% unique(other_transcripts$transcript_id) &
    annotation$type == "transcript"]
other_transcripts[
  data.table(
    transcript_id = used_other_transcripts$transcript_id,
    transcript_start = start(used_other_transcripts),
    transcript_end = end(used_other_transcripts)
  ),
  on = .NATURAL,
  c("transcript_start", "transcript_end") := .(transcript_start, transcript_end)
]

# used transcripts are the ones that include the alternative regions, i.e., that are part of the alternative transcripts
event_dt[event_transcripts[, .(
  alternative_start = list(transcript_start),
  alternative_end = list(transcript_end),
  gene_start = min(transcript_start),
  gene_end = max(transcript_end)
), by = ID], on = .NATURAL, c(
  "alternative_start",
  "alternative_end",
  "gene_start",
  "gene_end"
) := .(alternative_start, alternative_end, gene_start, gene_end)]
event_dt[other_transcripts[, .(
  reference_start = list(transcript_start),
  reference_end = list(transcript_end)
), by = ID], on = .NATURAL, c("reference_start", "reference_end") := .(reference_start, reference_end)]

# add scores to event_dt
ss_dt <- merge(scores3, scores5, by = "ID")
event_dt[ss_dt, on = "ID", c("3ss", "5ss") := .(i.3ss, i.5ss)]

# add distance to TSS and TES
event_dt[, distance_TSS := ifelse(strand == "-",
  log2(gene_end - end(event_gr[ID])),
  log2(start(event_gr[ID]) - gene_start)
)]
event_dt[, distance_TES := ifelse(strand == "-",
  log2(start(event_gr[ID]) - gene_start),
  log2(gene_end - end(event_gr[ID]))
)]

# add region_lengths
event_dt[, paste("width", "upstream_other_region", sep = ";") := log2(width(upstream_gr))]
event_dt[, paste("width", "event_name", sep = ";") := log2(width(event_gr))]
event_dt[, paste("width", "downstream_other_region", sep = ";") := log2(width(downstream_gr))]
```

```{r, load-ChromHMM}
# load ChromHMM states
activeChromHMM <-
  rtracklayer::import("data/StackedChromHMM_V1_EnhancerStates.bed.gz") # from IHEC/post_processed_data/Analysis/IHEC_gABC_Interactions/StackedChromHMM_V1_EnhancerStates.bed.gz
activeChromHMM$chrom_state <- activeChromHMM$name
# give them an name matching the index
activeChromHMM$name <-
  sprintf("chromhmm_%d", seq_along(activeChromHMM))
# find overlaps with the events
chromhmm_hits <-
  findOverlaps(event_gr,
    activeChromHMM,
    maxgap = vicinity,
    ignore.strand = TRUE
  )
chromhmm_in_vicinity <-
  sort(unique(to(chromhmm_hits)[from(chromhmm_hits) %in% keep_rows_manual]))

# give the alternative and adjacent regions names for the bed file
event_gr$name <- event_dt[, ID]
upstream_gr$name <- sprintf("up_%d", event_dt[, ID])
downstream_gr$name <- sprintf("down_%d", event_dt[, ID])

# write the bed file over which averaging should be done:
aggregateOver <- c(
  event_gr[keep_rows_manual],
  upstream_gr[keep_rows_manual],
  downstream_gr[keep_rows_manual],
  activeChromHMM[chromhmm_in_vicinity]
)
rtracklayer::export(
  aggregateOver,
  "processed_data/aggregateOver.bed"
)
```


```{r, save-workspace}
save.image("processed_data/aggregating.rda")
```
